<div class="container">
    <div class="row">
        <p>Who will you challenge?</p>
    </div>
    <div class="row">
        <div class="col-6 mx-auto text-left">
            <p> Monster 1 {{{monster.id}}}</p>
            <button class="btn btn-primary" type="submit">select</button>
        </div>
        <div class="col-6 mx-auto text-left">
            <p> Monster 2 {{{monster.id}}}</p>
            <button class="btn btn-primary" type="submit">select</button>
        </div>
    </div>
    <div class="row">
        <div class="col-6 mx-auto text-left">
            <p> Monster 3 {{{monster.id}}}</p>
            <button class="btn btn-primary" type="submit">select</button>
        </div>
        <div class="col-6 mx-auto text-left">
            <p> Monster 4 {{{monster.id}}}</p>
            <button class="btn btn-primary" type="submit">select</button>
        </div>
    </div>
</div>